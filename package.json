{
  "name": "blog",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "graphql-deploy": "turbo run graphql-deploy",
    "lint": "turbo run lint",
    "format": "turbo run format"
  },
  "devDependencies": {
    "eslint-config-custom": "*",
    "prettier-config-custom": "*",
    "turbo": "latest"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
